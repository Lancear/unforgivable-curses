Decided it was a good idea to generate a svg instead of building a frontend with a charting library.
The result is a svg which has multiple tabs (created with animations) and displays the fancy graphs.
Even created a fancy xml builder to generate the svg.